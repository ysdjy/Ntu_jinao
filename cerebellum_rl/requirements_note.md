# requirements note

- 本任务优先复用 Isaac Lab 现有安装环境，不额外新增 pip 依赖。
- 需要可用的 Isaac Lab 运行环境（例如你当前的 `env_isaaclab`）。
- 训练依赖 Isaac Lab 自带 `isaaclab_rl` 与 `rsl-rl-lib`。
- 若缺失可用版本，使用：
  - `./isaaclab.sh -p -m pip install rsl-rl-lib`

